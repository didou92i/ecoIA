name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
      - name: Install locked dependencies
        run: npm ci
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Format
        run: npm run format:check
      - name: Lint
        run: npm run lint
      - name: Type-check
        run: npm run typecheck
      - name: Unit and adapter tests
        run: npm test
      - name: Reproducible impact coefficients
        run: npm run impact-coefficients
      - name: Quantitative source freshness
        run: npm run source-freshness
      - name: Chromium extension journeys
        run: npm run e2e
      - name: Full dependency tree audit
        run: npm run audit
      - name: High-confidence secret scan
        run: npm run secrets
      - name: Dual browser packages
        run: npm run build
      - name: Archive size budget
        run: npm run size
      - name: Release checksums
        run: npm run checksums
